<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:p="http://www.springframework.org/schema/p"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd">
	
	<!-- 
		bean  id : 해당 빈을 다른 빈과 구분하기 위한 구분자(유니크)
		      name : id와 동반 선언되면 id의 별명
		             name만 선언되면 id와 동일
	 -->
	<bean name="secondController" class="kr.or.ddit.second.controller.SecondController"></bean>
	
	<!-- property(p namespace) : 빈으로 등록된 클래스 내 setter에 값 주입.
	     scope : singlton(default)
	                 스프링 설정파일 내에 선언된 빈 등록 클래스는 싱글톤 패턴을 적용해서 인스턴스관리
	             prototype
	                 빈 클래스의 인스턴스의 DI 처리시 신규 인스턴스화를 진행 후 주입.
	             request
	                클라이언트의 요청과 응답 전송 범위 내에서 빈 클래스는 싱글톤 패턴을 적용해서 인스턴스
	                관리
	             session
	                단일 세션 내에서 빈 클래스는 싱글톤 패턴을 적용해서 인스턴스 관리
	  -->
</beans>





